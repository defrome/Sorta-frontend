import 'package:flutter/material.dart';

class SortaColors {
  static const background = Color(0xFF000000);
  static const primary = Color(0xFFFFFFFF);
  static const secondary = Color(0xFFA1A1AA);
  static const card = Color.fromRGBO(255, 255, 255, 0.04);
  static const border = Color.fromRGBO(255, 255, 255, 0.10);
  static const divider = Color.fromRGBO(255, 255, 255, 0.06);
  static const active = Color.fromRGBO(255, 255, 255, 0.08);
}
